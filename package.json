{
  "name": "chat-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd public && npm install && npm run build"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  }
}